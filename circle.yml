---
machine:
  ruby:
    version: 2.2.3
  services:
    - docker

dependencies:
  override:
    - bundle install --binstubs

test:
  override:
    -  KITCHEN_YAML=.kitchen.circle.yml bin/kitchen test
